<div class="health_container">
  <div class="row">
    <div id="main">
      <ul>
      <% @health_issues.each do |health_issue| %>
      <li>
    <p> <%= image_tag attachment_url(health_issue, :photo, :fill, 180, 180, format: "jpg") %></p>
  <p id="name"><%= health_issue.name %></p></li>

      <% end %>
    </ul>

      <%= react_component('CheckboxInput') %>

    </div>
  </div>
